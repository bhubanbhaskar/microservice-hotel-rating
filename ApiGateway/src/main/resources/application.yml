server:
  port: 8084

spring:
  application:
    name: API-GATEWAY
  cloud:
    gateway:
      routes:
        - id: USER-SERVICE
          uri: lb://USER-SERVICE
          predicates:
            - Path= /users/**

        - id: HOTEL-SERVICE
          uri: lb://HOTEL-SERVICE
          predicates:
            - Path= /hotels/**, /staffs/**

        - id: RATING-SERVICE
          uri: lb://RATING-SERVICE
          predicates:
            - Path= /ratings/**
  config:
    import: optional:configserver:http://localhost:8085
okta:
  oauth2:
    issuer: https://dev-45924573.okta.com/oauth2/default
    audience: api://default
    client-id: 0oaa4ow22ph2wpbzz5d7
    client-secret: kL6mBUkyfbWvNKw79-FzAzAfyUdt-ao55G5N4oMk
    scopes: openid, profile, email, offline_access

#eureka:
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://localhost:8761/eureka
#  instance:
#    prefer-ip-address: true